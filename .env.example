PORT=3000
# 127.0.0.1 = bind localhost + restrict API/SSE/MCP to local clients
# 0.0.0.0     = bind all interfaces, no API/SSE/MCP restriction
HOST=127.0.0.1
DB_PATH=data.sqlite
# Subpath when served behind a reverse proxy (e.g. nginx location /news). No trailing slash.
# BASE_PATH=/news
